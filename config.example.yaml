plugins:
  enabled: true
  dir: "plugins"
  configs:
    freebuff:
      enabled: true
      priority: 100
      # Default OAuth on CPA /oauth: freebuff → "Freebuff OAuth", codebuff → "Codebuff OAuth"
      login_mode: freebuff
      debug: false

api-keys:
  - "sk-local"
